# Local config & secrets (never commit these)
config.yaml
config.local.yaml
*.local.yaml
watch-later.yaml
cookies.txt
cookies,txt
.env
.env.*
*.pem

# Downloaded binaries
yt-dlp
playlist-streamer/playlist-streamer
owncast/owncast
layer-server/layer-server

# Runtime data
data/
owncast/data/
playlist-streamer/data/
playlist-streamer/videos/
playlist-streamer/playlists/tmp/
playlist-streamer/playlists/old/

# Build artifacts
app/

# IDE / editor
.cursor/

# Local config (may contain stream key, API tokens)
playlist-streamer/config.yaml

# User-specific playlists (generated from YouTube watch later, etc.)
playlist-streamer/watch-later.yaml
